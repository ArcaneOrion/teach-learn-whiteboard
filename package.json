{
  "name": "teach-learn-whiteboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "共写白板 —— 安卓本地学习白板（M0：先做能画线的网页版）",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^30.0.1",
    "typescript": "^5.9.3",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "dompurify": "^3.4.15"
  }
}
